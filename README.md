# `@evaldosantos/prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @evaldosantos/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@evaldosantos/prettier-config"
}
```
